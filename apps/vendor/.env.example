# FLOQ Merchant app (Expo) environment.
# Copy to .env before building the APK.

# The backend the APK talks to. MUST be your deployed HTTPS host for a real
# device on mobile data (localhost only works in the emulator).
EXPO_PUBLIC_API_URL=https://your-floq-host.example.com
