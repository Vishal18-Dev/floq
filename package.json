{
  "name": "floq-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "FLOQ - Mobile-first commerce operating system for high-volume physical merchants in India",
  "workspaces": [
    "packages/*",
    "backend",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:vendor\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:vendor": "npm run dev --workspace=apps/vendor",
    "build": "npx esbuild backend/src/server.ts --bundle --platform=node --target=node20 --outdir=backend/dist --external:pg-native --external:better-sqlite3 && npx esbuild backend/src/db/migrate.ts --bundle --platform=node --target=node20 --outdir=backend/dist/db --external:pg-native --external:better-sqlite3",
    "build:all": "npm run build --workspaces --if-present",
    "test": "npm run test --workspace=backend",
    "seed": "npm run seed --workspace=backend"
  },
  "dependencies": {
    "@types/node": "^20.14.9",
    "esbuild": "^0.23.0",
    "typescript": "^5.4.5"
  },
  "keywords": [
    "floq",
    "pos",
    "merchant",
    "qsr",
    "india",
    "upi",
    "queue"
  ],
  "author": "FLOQ Technologies",
  "license": "MIT"
}
